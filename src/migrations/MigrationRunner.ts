export interface Migration{from:number;to:number;description:string;run():Promise<void>}export class MigrationRunner{constructor(private current:number,private migrations:Migration[]){}async runTo(target:number){let v=this.current;for(const m of [...this.migrations].sort((a,b)=>a.from-b.from)){if(m.from===v&&m.to<=target){await m.run();v=m.to}}if(v!==target)throw new Error(`Migration incomplète ${v} -> ${target}`);return v}}
