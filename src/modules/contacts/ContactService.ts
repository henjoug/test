import type { Contact } from '../../domain/types.js';import type { DataService,SequenceService,AuditService } from '../../services/contracts.js';export class ContactService{constructor(private data:DataService,private seq:SequenceService,private audit:AuditService,private instanceId:string){}async list(affaireId:string){return this.data.search<Contact&Record<string,unknown>>('CONTACTS',c=>c.INSTANCE_ID===this.instanceId&&c.ID_AFFAIRE===affaireId&&!c.ARCHIVED_AT);}async create(affaireId:string,input:Partial<Contact>){const now=new Date().toISOString();const ID_CONTACT=await this.seq.next('CON');const r:Contact={ID_CONTACT,INSTANCE_ID:this.instanceId,ID_AFFAIRE:affaireId,NOM:input.NOM||'Contact',PRENOM:input.PRENOM,EMAIL:input.EMAIL,TELEPHONE:input.TELEPHONE,FONCTION:input.FONCTION,STATUT:'ACTIF',DATE_CREATION:now,DATE_MODIFICATION:now};await this.data.create('CONTACTS',r as unknown as Record<string,unknown>);await this.audit.log('CONTACT_CREATED','CONTACT',ID_CONTACT);return r;}}
