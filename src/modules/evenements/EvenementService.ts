import type { Evenement } from '../../domain/types.js';import type { DataService,SequenceService,AuditService } from '../../services/contracts.js';export class EvenementService{constructor(private data:DataService,private seq:SequenceService,private audit:AuditService,private instanceId:string,private userId:string){}async list(affaireId:string){return this.data.search<Evenement&Record<string,unknown>>('EVENEMENTS',e=>e.INSTANCE_ID===this.instanceId&&e.ID_AFFAIRE===affaireId&&!e.ARCHIVED_AT);}async create(affaireId:string,titre:string,description='',type='NOTE'){const now=new Date().toISOString();const ID_EVENEMENT=await this.seq.next('EVT');const r:Evenement={ID_EVENEMENT,INSTANCE_ID:this.instanceId,ID_AFFAIRE:affaireId,TYPE:type,TITRE:titre,DESCRIPTION:description,DATE_EVENEMENT:now,SOURCE:'CA_OS_APP',CREATED_BY_USER_ID:this.userId,DATE_CREATION:now,DATE_MODIFICATION:now};await this.data.create('EVENEMENTS',r as unknown as Record<string,unknown>);await this.audit.log('EVENT_CREATED','EVENEMENT',ID_EVENEMENT);return r;}}
